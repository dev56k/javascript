// ES5
function exampleFunction() {
    return "Hello";
}

// ES6
const functionArrow = () => "Hello"

exampleFunction();

console.log(functionArrow());

/* 
Example REACT

function ListTask () {
    return (
        <ul>
            {lists.map(element => (<li>{element.name}</li>))}
        </ul>
    );
}

*/